<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- ═══════════════════════════════════════════════════════
                 OAuth callback deep link
                 Handles:  com.flanorx.app://oauth_callback?access_token=...
                 Triggered by Google redirecting back after sign-in in
                 the system browser. Capacitor's App.addListener("appUrlOpen")
                 receives this URL.
                 ═══════════════════════════════════════════════════════ -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.flanorx.app"
                    android:host="oauth_callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Firebase Cloud Messaging default notification channel + icon
             (optional but recommended — see notes below) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="flanorx_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>

    <!-- ═══════════════════════════════════════════════════════
         PERMISSIONS
         ═══════════════════════════════════════════════════════ -->

    <!-- Networking (required for API + Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ═══ Push notifications ═══════════════════════════════ -->

    <!-- Android 13+ (API 33) — runtime permission for showing notifications.
         Without this, FCM messages arrive but are silently dropped. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibrate when a notification arrives (optional, harmless) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Re-arm the FCM token listener after device reboot (optional) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Avoid killing the app process while a foreground push handler runs -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ═══ Camera & media ═══════════════════════════════════ -->

    <!-- Direct camera access for @capacitor/camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Read images from gallery / photos:
         - Android 13+ (API 33+): READ_MEDIA_IMAGES
         - Android 12 and below:   READ_EXTERNAL_STORAGE (limited to API 32) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Write to storage — only needed on very old devices (Android 9 and below).
         The Capacitor Camera plugin uses FileProvider for saving, so this is
         capped at API 28 to satisfy legacy installs without over-asking. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ═══ Location (required for @capacitor/geolocation) ═══ -->

    <!-- Coarse location: network/Wi-Fi based (approximate).
         Required for Android 12+ even when you only want fine location. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Fine location: GPS based (precise).
         This is what enableHighAccuracy:true uses. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- ═══ Feature declarations ═════════════════════════════ -->
    <!-- Mark camera as optional so the app is still installable on
         devices without a camera (e.g. some emulators, tablets). -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- Declare GPS as optional so devices without GPS hardware
         (rare, but possible) can still install the app. -->
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" />

</manifest>